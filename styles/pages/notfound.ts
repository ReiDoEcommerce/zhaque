import styled from "styled-components";

export const NotFound = styled.section`
    padding: 10vw 0;
    text-align: center;

    p {
        margin: 0;
        color: var(--secundary-color);
    }

`